<Window x:Class="GestionMC.Desktop.FacturasMesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Facturas por mes"
        Height="640"
        Width="1180"
        WindowStartupLocation="CenterScreen"
        Background="#FFF4F5F7">

    <Window.Resources>
        <SolidColorBrush x:Key="PageBackground" Color="#FFF4F5F7"/>
        <SolidColorBrush x:Key="CardBackground" Color="White"/>
        <SolidColorBrush x:Key="BorderBrushLight" Color="#FFCED4DA"/>

        <Style x:Key="CoNumeroCellStyle" TargetType="DataGridCell">
            <Setter Property="Background" Value="{x:Null}"/>
            <Setter Property="Foreground" Value="Black"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding CoNumeroDisplay}" Value="NO VALIDADA">
                    <Setter Property="Background" Value="#FFFFD6D6"/>
                </DataTrigger>

                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding CoNumeroDisplay}" Value="SIN REGISTRO"/>
                        <Condition Binding="{Binding ProveedorExiste}" Value="True"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="#FFFFF2CC"/>
                </MultiDataTrigger>

                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding CoNumeroDisplay}" Value="NO REGISTRADA"/>
                        <Condition Binding="{Binding ProveedorExiste}" Value="True"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="#FFFFF2CC"/>
                </MultiDataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PmNrosecCellStyle" TargetType="DataGridCell">
            <Setter Property="Background" Value="{x:Null}"/>
            <Setter Property="Foreground" Value="Black"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding PmNrosecDisplay}" Value="NO VALIDADA">
                    <Setter Property="Background" Value="#FFFFD6D6"/>
                </DataTrigger>

                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding PmNrosecDisplay}" Value="SIN REGISTRO"/>
                        <Condition Binding="{Binding ProveedorExiste}" Value="True"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="#FFFFF2CC"/>
                </MultiDataTrigger>

                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding PmNrosecDisplay}" Value="NO REGISTRADA"/>
                        <Condition Binding="{Binding ProveedorExiste}" Value="True"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="#FFFFF2CC"/>
                </MultiDataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="373*"/>
            <ColumnDefinition Width="201*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="12" Background="White" BorderBrush="#FFCED4DA" BorderThickness="1" CornerRadius="4" Margin="0,0,0,10" Grid.ColumnSpan="2">
            <Grid VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Año" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,4"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="Mes" FontWeight="SemiBold" FontSize="14" Margin="12,0,0,4"/>

                <ComboBox Grid.Row="1" Grid.Column="0" x:Name="CmbYear" Height="32" FontSize="14" IsEditable="True" IsReadOnly="True"/>
                <ComboBox Grid.Row="1" Grid.Column="1" x:Name="CmbMonth" Height="32" FontSize="14" IsEditable="True" IsReadOnly="True" Margin="12,0,0,0"/>

                <Button Grid.Row="1" Grid.Column="2" Content="Cargar mes" Width="140" Height="32" FontSize="14" Click="BtnCargar_Click" Margin="24,0,0,0" Grid.ColumnSpan="2" HorizontalAlignment="Left"/>
            </Grid>
        </Border>

        <DataGrid x:Name="GridFacturas"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserSortColumns="False"
                  CanUserResizeColumns="False"
                  CanUserResizeRows="False"
                  FontSize="14" Grid.ColumnSpan="2">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ProveedorExiste}" Value="True">
                            <Setter Property="Background" Value="#FFDFF6D8"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <!--<DataGridTextColumn Header="#" Binding="{Binding Nro}" Width="50"/>-->
                <DataGridTextColumn Header="Fecha" Binding="{Binding FechaEmision}" Width="110"/>
                <DataGridTextColumn Header="RUC" Binding="{Binding RucEmisor}" Width="140"/>
                <DataGridTextColumn Header="Proveedor" Binding="{Binding RazonSocialEmisor}" Width="*"/>
                <DataGridTextColumn Header="N° Factura" Binding="{Binding NumeroFactura}" Width="140"/>
                <DataGridTextColumn Header="N° de compra" Binding="{Binding CoNumeroDisplay}" Width="120" CellStyle="{StaticResource CoNumeroCellStyle}"/>
                <DataGridTextColumn Header="N° de retención" Binding="{Binding PmNrosecDisplay}" Width="130" CellStyle="{StaticResource PmNrosecCellStyle}"/>
                <DataGridTemplateColumn Header="Ver" Width="70">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Width="55" Height="26" Click="BtnView_Click" FontFamily="Segoe MDL2 Assets">
                                <TextBlock Text="&#xE8A0;" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="2" Margin="0,10,0,0" Padding="8" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="4" Grid.ColumnSpan="2">
            <TextBlock x:Name="LblStatus" Text="Listo." TextWrapping="Wrap"/>
        </Border>
    </Grid>
</Window>

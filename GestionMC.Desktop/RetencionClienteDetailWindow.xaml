<Window x:Class="GestionMC.Desktop.RetencionClienteDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Detalle de retención"
        Height="640"
        Width="1250"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#FFF4F5F7">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="12" Background="White" BorderBrush="#FFCED4DA" BorderThickness="1" CornerRadius="6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Vertical" Margin="0" >
                    <TextBlock Text="Cliente (sujeto retenido)" FontWeight="SemiBold"/>
                    <TextBlock Text="Nombre:" FontWeight="SemiBold"/>
                    <TextBlock x:Name="LblCliente"/>
                    <TextBlock Text="Identificación:" FontWeight="SemiBold" Margin="0,4,0,0"/>
                    <TextBlock x:Name="LblIdentificacion"/>
                    <TextBlock Text="Tipo identificación:" FontWeight="SemiBold" Margin="0,4,0,0"/>
                    <TextBlock x:Name="LblTipoIdent"/>
                    <TextBlock Text="Fecha emisión retención:" FontWeight="SemiBold" Margin="0,4,0,0"/>
                    <TextBlock x:Name="LblFechaEmision"/>
                    <TextBlock Text="Periodo fiscal:" FontWeight="SemiBold" Margin="0,4,0,0"/>
                    <TextBlock x:Name="LblPeriodoFiscal"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Vertical" Margin="0" >
                    <TextBlock Text="Datos de autorización" FontWeight="SemiBold"/>
                    <TextBlock Text="No.:" FontWeight="SemiBold"/>
                    <TextBlock x:Name="LblNumero"/>
                    <TextBlock Text="Número de autorización:" FontWeight="SemiBold" Margin="0,4,0,0"/>
                    <TextBlock x:Name="LblNumeroAutorizacion" TextWrapping="Wrap"/>
                    <TextBlock Text="Fecha y hora autorización:" FontWeight="SemiBold" Margin="0,4,0,0"/>
                    <TextBlock x:Name="LblFechaAut"/>
                    <TextBlock Text="Estado:" FontWeight="SemiBold" Margin="0,4,0,0"/>
                    <TextBlock x:Name="LblEstado"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Padding="10" Margin="0,10,0,6" Background="White" BorderBrush="#FFCED4DA" BorderThickness="1" CornerRadius="6">
            <StackPanel>
                <TextBlock Text="Detalle de retenciones" FontWeight="SemiBold" Margin="0,0,0,6"/>
                <DataGrid x:Name="GridDetalles"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserResizeRows="False"
                          CanUserSortColumns="False"
                          CanUserResizeColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Comprobante" Binding="{Binding Comprobante}" Width="110"/>
                        <DataGridTextColumn Header="Número" Binding="{Binding Numero}" Width="150"/>
                        <DataGridTextColumn Header="Fecha Emisión" Binding="{Binding FechaEmision}" Width="110"/>
                        <DataGridTextColumn Header="Ejercicio Fiscal" Binding="{Binding EjercicioFiscal}" Width="110"/>
                        <DataGridTextColumn Header="Base Imponible" Binding="{Binding BaseImponible}" Width="120"/>
                        <DataGridTextColumn Header="Impuesto" Binding="{Binding Impuesto}" Width="150"/>
                        <DataGridTextColumn Header="% Retención" Binding="{Binding Porcentaje}" Width="110"/>
                        <DataGridTextColumn Header="Valor retenido" Binding="{Binding ValorRetenido}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>

        <Border Grid.Row="3" Padding="10" Background="White" BorderBrush="#FFCED4DA" BorderThickness="1" CornerRadius="6" Margin="0,10,0,0">
            <TextBlock x:Name="LblStatus" Text="Listo." TextWrapping="Wrap"/>
        </Border>
    </Grid>
</Window>
